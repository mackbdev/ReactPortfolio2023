class SkillsFormat {
	constructor(title, start, info, status) {
		{
			this.title = title;
			this.start = start;
			this.info = info;
			this.status = status;
		}
	}
}

module.exports = SkillsFormat;
